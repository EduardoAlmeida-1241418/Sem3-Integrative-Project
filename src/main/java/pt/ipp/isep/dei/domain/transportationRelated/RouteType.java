package pt.ipp.isep.dei.domain.transportationRelated;

/**
 * Enumeration that represents the type of a route.
 * A route can be simple or complex depending on its structure.
 */
public enum RouteType {

    /** Represents a simple route */
    SIMPLE,
    /** Represents a complex route */
    COMPLEX
}
